---
phase: 06-route-to-market
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified: []
autonomous: false
requirements:
  - MKTG-01
  - MKTG-02

must_haves:
  truths:
    - "bolusbrain.app is live over HTTPS with a valid certificate"
    - "Email submitted on the live site is captured in Loops.so contacts list"
    - "Both iPhone mockup demo screenshots and the Dexcom teaser are visible without excessive scrolling on a 1280x800 desktop browser"
    - "The LOOPS_API_KEY Vercel environment variable is set and the subscribe endpoint works in production"
  artifacts:
    - path: "https://bolusbrain.app"
      provides: "Live landing page accessible over HTTPS"
    - path: "Vercel project settings → Environment Variables → LOOPS_API_KEY"
      provides: "Server-side API key for production email capture"
  key_links:
    - from: "bolusbrain-landing GitHub repo"
      to: "Vercel project"
      via: "Vercel Git integration (push to main = deploy)"
    - from: "Vercel project"
      to: "bolusbrain.app DNS"
      via: "A record 76.76.21.21 + CNAME for www"
---

<objective>
Deploy the bolusbrain-landing Next.js project to Vercel, configure the LOOPS_API_KEY production environment variable, connect the bolusbrain.app custom domain, and verify the live site works end-to-end.

Purpose: Ships the landing page to production and closes MKTG-01 and MKTG-02 by making the live page accessible and the email capture functional.
Output: bolusbrain.app is live, HTTPS, email capture working.
</objective>

<execution_context>
@C:/Users/Liamb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Liamb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-CONTEXT.md
@C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-RESEARCH.md
@C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialise git repo and deploy to Vercel</name>
  <read_first>
    - C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-01-SUMMARY.md (confirm .env.local is gitignored)
    - C:/Users/Liamb/bolusbrain-landing/.gitignore (verify .env*.local present)
  </read_first>
  <files>C:/Users/Liamb/bolusbrain-landing/.gitignore</files>
  <action>
Step 1 — Verify .env.local is gitignored before any git operations:
```bash
cd C:/Users/Liamb/bolusbrain-landing
grep "env" .gitignore
```
Expected: a line matching `.env*.local`. If missing, ADD it now before proceeding. Do not commit until this is confirmed.

Step 2 — Initialise git and make first commit:
```bash
cd C:/Users/Liamb/bolusbrain-landing
git init
git add -A
git status  # VERIFY: .env.local does NOT appear in the staged files list
git commit -m "feat: initial BolusBrain landing page

- Next.js 15 App Router + TypeScript + Tailwind CSS
- Hero section with email capture form (Loops.so via /api/subscribe)
- Demo section with iPhone mockup frames and Dexcom teaser
- Dark theme throughout
- Placeholder screenshots — replace with actual app screenshots before promotion"
```

Step 3 — Create GitHub repository and push:
```bash
gh repo create bolusbrain-landing --public --source=. --remote=origin --push
```
If gh CLI is not authenticated, run `gh auth login` first.

Step 4 — Deploy to Vercel:
```bash
npx vercel --yes
```
This links the project to Vercel and deploys. Accept all defaults. The deploy URL will look like `bolusbrain-landing-xxx.vercel.app`.

Step 5 — Set LOOPS_API_KEY in Vercel environment variables:
```bash
npx vercel env add LOOPS_API_KEY production
```
When prompted, paste the LOOPS_API_KEY value from https://app.loops.so → Settings → API.

Then redeploy to pick up the new env var:
```bash
npx vercel --prod
```

Step 6 — Verify the subscribe endpoint works in production:
```bash
curl -X POST https://bolusbrain-landing-xxx.vercel.app/api/subscribe \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}'
```
Expected response: `{"success":true}`

If the response is `{"error":"Service unavailable"}`, the LOOPS_API_KEY is not set in Vercel — repeat Step 5 and redeploy.
  </action>
  <verify>
    <automated>cd C:/Users/Liamb/bolusbrain-landing && git log --oneline -1 && git status && echo "Git repo: OK" && npx vercel ls 2>/dev/null | head -5 && echo "Vercel: connected"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local` is NOT in `git status` staged or tracked files
    - Git repo initialised with at least one commit
    - `npx vercel ls` shows the bolusbrain-landing project
    - Vercel deploy URL (bolusbrain-landing-xxx.vercel.app) returns HTTP 200
    - `curl -X POST [deploy-url]/api/subscribe -H "Content-Type: application/json" -d '{"email":"liam@example.com"}'` returns `{"success":true}`
  </acceptance_criteria>
  <done>Code deployed to Vercel. Subscribe endpoint working in production with LOOPS_API_KEY set.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify live site — fold layout, email capture, domain setup</name>
  <files></files>
  <action>Human verification required — see how-to-verify below. Claude has completed all automated deployment steps.</action>
  <verify>
    <automated>HUMAN-VERIFY — automated checks not applicable for visual and DNS verification</automated>
  </verify>
  <done>User has confirmed: fold layout correct, email capture working end-to-end, domain connected or DNS changes submitted.</done>
  <what-built>
    Full landing page deployed to Vercel production URL. Email capture endpoint live. Need to:
    1. Visually verify fold layout on desktop
    2. Test email capture end-to-end on the live site
    3. Connect custom domain (bolusbrain.app) — requires DNS changes in your registrar
  </what-built>
  <how-to-verify>
    Step 1 — Visual fold check (2 minutes):
    Open the Vercel deploy URL in a browser at 1280x800 window size (or use Chrome DevTools responsive mode set to 1280x800).
    Confirm WITHOUT scrolling:
    - BolusBrain headline and tagline visible
    - Email input and "Join waitlist" button visible
    - When you scroll ONE time (one page down), the demo screenshots and Dexcom "Coming soon" card are visible

    Step 2 — Email capture test (2 minutes):
    Enter your real email address in the form on the live site.
    Click "Join waitlist".
    Expected: "You're on the list!" appears inline where the button was.
    Then go to https://app.loops.so → Contacts and verify your email appears.

    Step 3 — Custom domain (5-10 minutes, requires DNS access):
    In Vercel dashboard (vercel.com → bolusbrain-landing project → Settings → Domains):
    - Add domain: `bolusbrain.app`
    - Add domain: `www.bolusbrain.app`
    Vercel will show the DNS records required. In your domain registrar's DNS panel:
    - For `bolusbrain.app` (apex): Add A record pointing to the IP shown in Vercel (typically 76.76.21.21)
    - For `www.bolusbrain.app`: Add CNAME pointing to `cname.vercel-dns.com`
    DNS propagation: 5 minutes to 48 hours. Vercel dashboard will show green checkmarks when verified.

    Step 4 — Confirm screenshots status:
    The page currently shows placeholder (grey) images in the iPhone mockups.
    When you have real screenshots from the Expo app, place them at:
    - C:/Users/Liamb/bolusbrain-landing/public/screenshots/placeholder-1.png (hero — HomeScreen)
    - C:/Users/Liamb/bolusbrain-landing/public/screenshots/placeholder-2.png (demo — MealLogScreen photo step)
    - C:/Users/Liamb/bolusbrain-landing/public/screenshots/placeholder-3.png (demo — carb estimate result)
    Then run `npx vercel --prod` to redeploy with real images.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — fold layout looks correct, email capture works, domain connected (or in progress)
    - "fold-issue [description]" — fold layout needs adjustment
    - "email-issue [description]" — email capture not working
    - "domain-pending" — domain changes submitted, waiting for DNS propagation
  </resume-signal>
</task>

</tasks>

<verification>
Phase 6 is complete when all of the following are TRUE:

MKTG-01:
- Landing page exists at bolusbrain.app (or Vercel URL while DNS propagates)
- Demo section with iPhone mockups showing carb estimation flow is present
- Dexcom "Coming soon" teaser is present
- Both are reachable within one scroll on a 1280x800 desktop browser

MKTG-02:
- Email form exists on the page
- Submitting a real email results in "You're on the list!" inline
- The email appears in Loops.so contacts dashboard
- LOOPS_API_KEY is set in Vercel production environment (not in any committed file)

LEGAL-01:
- .planning/phases/06-route-to-market/MHRA-correspondence/2026-03-18-initial-enquiry.md exists
- Documents date sent, address (devices@mhra.gov.uk), content summary, and next steps
</verification>

<success_criteria>
- bolusbrain.app resolves over HTTPS (may take up to 48 hours for DNS)
- Email submission on live site captured in Loops.so — end-to-end verified by user
- LOOPS_API_KEY in Vercel env vars only — not in any git-tracked file
- MHRA correspondence record committed to .planning/
- Phase 6 closed: MKTG-01, MKTG-02, LEGAL-01 all marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-03-SUMMARY.md`

Include:
- Vercel deploy URL
- Custom domain status (connected / DNS pending / not yet started)
- Email capture verified: yes/no
- Screenshot status: placeholders / real images
- LOOPS_API_KEY: set in Vercel / pending user action
- Any issues encountered during deployment

Also update REQUIREMENTS.md:
- MKTG-01: mark complete with live URL
- MKTG-02: mark complete with confirmation email capture works
- LEGAL-01: already marked complete — confirm correspondence file path
</output>
