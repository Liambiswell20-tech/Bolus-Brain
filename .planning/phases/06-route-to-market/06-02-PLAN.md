---
phase: 06-route-to-market
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - C:/Users/Liamb/bolusbrain-landing/src/components/IPhoneMockup.tsx
  - C:/Users/Liamb/bolusbrain-landing/src/components/EmailForm.tsx
  - C:/Users/Liamb/bolusbrain-landing/src/components/sections/Hero.tsx
  - C:/Users/Liamb/bolusbrain-landing/src/components/sections/DemoSection.tsx
  - C:/Users/Liamb/bolusbrain-landing/src/components/sections/Footer.tsx
  - C:/Users/Liamb/bolusbrain-landing/src/app/page.tsx
autonomous: true
requirements:
  - MKTG-01
  - MKTG-02

must_haves:
  truths:
    - "Hero section fills one full viewport height on desktop (1280x800) with tagline, email form, and hero iPhone mockup all visible without scrolling"
    - "Demo section is the second viewport — two iPhone mockups and the Dexcom teaser card are visible at the top of it without scrolling into it"
    - "Email form submits to /api/subscribe and shows 'You're on the list!' inline on success"
    - "Email form shows inline error message on failure — no page redirect"
    - "Dexcom teaser shows a 'Coming soon' badge and one sentence description — no false promises"
    - "Page has dark background (bg-gray-950) throughout — no white flash or light sections"
  artifacts:
    - path: "C:/Users/Liamb/bolusbrain-landing/src/components/IPhoneMockup.tsx"
      provides: "Reusable phone frame component"
      exports: ["IPhoneMockup"]
    - path: "C:/Users/Liamb/bolusbrain-landing/src/components/EmailForm.tsx"
      provides: "Client-side email capture form with inline state"
      exports: ["EmailForm"]
    - path: "C:/Users/Liamb/bolusbrain-landing/src/components/sections/Hero.tsx"
      provides: "Above-fold hero section"
      exports: ["Hero"]
    - path: "C:/Users/Liamb/bolusbrain-landing/src/components/sections/DemoSection.tsx"
      provides: "Demo + Dexcom teaser section"
      exports: ["DemoSection"]
    - path: "C:/Users/Liamb/bolusbrain-landing/src/app/page.tsx"
      provides: "Root page composing all sections"
  key_links:
    - from: "EmailForm.tsx"
      to: "/api/subscribe"
      via: "fetch POST in handleSubmit"
      pattern: "fetch.*api/subscribe"
    - from: "IPhoneMockup.tsx"
      to: "public/screenshots/placeholder-*.png"
      via: "img src prop"
      pattern: "src.*screenshots"
    - from: "page.tsx"
      to: "Hero, DemoSection, Footer"
      via: "direct import and render"
      pattern: "import.*Hero|DemoSection|Footer"
---

<objective>
Build all page components and sections for the BolusBrain landing page — IPhoneMockup frame, EmailForm with inline state, Hero, DemoSection (with Dexcom teaser), and Footer — then compose them in page.tsx.

Purpose: This is the visible product. The fold layout (MKTG-01) and working email form (MKTG-02 frontend) both live here.
Output: A fully rendered landing page at localhost:3000 — dark themed, fold-correct, email capture working.
</objective>

<execution_context>
@C:/Users/Liamb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Liamb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-CONTEXT.md
@C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-RESEARCH.md
@C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-01-SUMMARY.md

<interfaces>
<!-- Contracts from plan 01 that this plan builds against -->

/api/subscribe internal contract:
  POST /api/subscribe
  Body: { "email": string }
  200 success: { "success": true }
  400 invalid: { "error": "Valid email required" }
  500/502/503 error: { "error": string }

EmailForm states:
  'idle' | 'submitting' | 'success' | 'error'
  Success renders: <p className="text-green-400 font-medium">You're on the list!</p>
  Error renders: inline <p className="text-red-400 text-sm"> with errorMsg

IPhoneMockup props:
  interface IPhoneMockupProps { src: string; alt: string }

Placeholder screenshots (created in plan 01):
  /screenshots/placeholder-1.png  — hero mockup
  /screenshots/placeholder-2.png  — demo mockup 1 (photo capture step)
  /screenshots/placeholder-3.png  — demo mockup 2 (AI carb estimate result)

Fold layout requirement (MKTG-01 hard constraint):
  Desktop (lg: breakpoint, ≥1024px width):
    - Hero section: min-h-screen with lg:grid-cols-2 (text+form left, mockup right)
    - Demo section: the top of this section must be visible without scrolling — use min-h-screen
    - DemoSection top row: two iPhone mockups + Dexcom teaser all in lg:grid-cols-3
    - iPhone mockup height MUST be capped: max-h-[480px] or similar — 600px mockup on 768px viewport clips
  Mobile (below lg:):
    - Hero: single column, tagline + form visible, mockup below (acceptable to scroll to mockup)
    - Demo: stacked
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IPhoneMockup and EmailForm components</name>
  <read_first>
    - C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-RESEARCH.md (Pattern 2: iPhone mockup exact Tailwind classes, Pattern 4: EmailForm exact code)
    - C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-01-SUMMARY.md (Tailwind version detected — v3 or v4)
    - C:/Users/Liamb/bolusbrain-landing/src/app/layout.tsx (confirm project exists)
  </read_first>
  <files>
    C:/Users/Liamb/bolusbrain-landing/src/components/IPhoneMockup.tsx,
    C:/Users/Liamb/bolusbrain-landing/src/components/EmailForm.tsx
  </files>
  <action>
First create the components directory:
```bash
mkdir -p C:/Users/Liamb/bolusbrain-landing/src/components/sections
```

CREATE C:/Users/Liamb/bolusbrain-landing/src/components/IPhoneMockup.tsx:
```tsx
interface IPhoneMockupProps {
  src: string
  alt: string
  className?: string
}

export function IPhoneMockup({ src, alt, className = '' }: IPhoneMockupProps) {
  return (
    <div
      className={`relative mx-auto border-gray-700 bg-gray-800 border-[14px] rounded-[2.5rem] w-[240px] shadow-2xl shadow-black/60 ${className}`}
    >
      {/* Notch */}
      <div className="w-[120px] h-[16px] bg-gray-800 top-0 rounded-b-[1rem] left-1/2 -translate-x-1/2 absolute z-10" />
      {/* Left buttons — volume up */}
      <div className="h-[36px] w-[3px] bg-gray-700 absolute -left-[17px] top-[96px] rounded-l-lg" />
      {/* Left buttons — volume down */}
      <div className="h-[36px] w-[3px] bg-gray-700 absolute -left-[17px] top-[140px] rounded-l-lg" />
      {/* Right button — power */}
      <div className="h-[52px] w-[3px] bg-gray-700 absolute -right-[17px] top-[112px] rounded-r-lg" />
      {/* Screen */}
      <div className="rounded-[2rem] overflow-hidden w-[212px] h-[458px]">
        {/* eslint-disable-next-line @next/next/no-img-element */}
        <img src={src} alt={alt} className="w-full h-full object-cover object-top" />
      </div>
    </div>
  )
}
```

Note on sizing: The mockup is 240px wide and 458px tall (screen) + 28px borders = ~486px total height. This fits within a 768px viewport with room for header, padding, and the demo section peek. The original research example used 300px/600px which clips on short viewports — use this smaller size.

CREATE C:/Users/Liamb/bolusbrain-landing/src/components/EmailForm.tsx:
```tsx
'use client'
import { useState } from 'react'

type FormStatus = 'idle' | 'submitting' | 'success' | 'error'

export function EmailForm() {
  const [email, setEmail] = useState('')
  const [status, setStatus] = useState<FormStatus>('idle')
  const [errorMsg, setErrorMsg] = useState('')

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setStatus('submitting')
    setErrorMsg('')

    try {
      const res = await fetch('/api/subscribe', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email }),
      })
      const data: { success?: boolean; error?: string } = await res.json()

      if (data.success) {
        setStatus('success')
      } else {
        setStatus('error')
        setErrorMsg(data.error ?? 'Something went wrong — please try again')
      }
    } catch {
      setStatus('error')
      setErrorMsg('Network error — please check your connection')
    }
  }

  if (status === 'success') {
    return (
      <p className="text-green-400 font-medium text-lg">
        You&apos;re on the list!
      </p>
    )
  }

  return (
    <div>
      <form onSubmit={handleSubmit} className="flex gap-2 flex-col sm:flex-row">
        <input
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="your@email.com"
          required
          disabled={status === 'submitting'}
          className="flex-1 rounded-lg bg-gray-800 border border-gray-700 px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-red-500 disabled:opacity-50"
        />
        <button
          type="submit"
          disabled={status === 'submitting'}
          className="rounded-lg bg-red-600 hover:bg-red-500 active:bg-red-700 px-6 py-3 font-semibold text-white disabled:opacity-50 transition-colors whitespace-nowrap"
        >
          {status === 'submitting' ? 'Joining...' : 'Join waitlist'}
        </button>
      </form>
      {status === 'error' && (
        <p className="text-red-400 text-sm mt-2">{errorMsg}</p>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd C:/Users/Liamb/bolusbrain-landing && test -f src/components/IPhoneMockup.tsx && test -f src/components/EmailForm.tsx && grep -q "'use client'" src/components/EmailForm.tsx && grep -q "api/subscribe" src/components/EmailForm.tsx && grep -q "rounded-\[2.5rem\]" src/components/IPhoneMockup.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/IPhoneMockup.tsx exists and exports `IPhoneMockup`
    - src/components/EmailForm.tsx exists and exports `EmailForm`
    - EmailForm.tsx begins with `'use client'`
    - EmailForm.tsx contains `fetch('/api/subscribe'` — calls own Route Handler, not Loops.so directly
    - EmailForm.tsx does NOT contain `loops.so` anywhere
    - EmailForm.tsx contains all four status states: `'idle'`, `'submitting'`, `'success'`, `'error'`
    - EmailForm.tsx renders `You're on the list!` (with entity or escaped apostrophe) in success state
    - IPhoneMockup.tsx does NOT import any external library — pure Tailwind classes only
    - `npx tsc --noEmit` passes for both files
  </acceptance_criteria>
  <done>IPhoneMockup and EmailForm created. Both TypeScript-valid. EmailForm calls /api/subscribe only.</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero, DemoSection, Footer sections and compose page.tsx</name>
  <read_first>
    - C:/Users/Liamb/bolusbrain-landing/src/components/IPhoneMockup.tsx (exact props interface)
    - C:/Users/Liamb/bolusbrain-landing/src/components/EmailForm.tsx (exact export name)
    - C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-RESEARCH.md (Pattern 3: fold layout guarantee — min-h-screen + lg:grid-cols-2, Pitfall 3: fold breaks on short viewports)
    - C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-CONTEXT.md (decisions: fold requirement, Dexcom teaser spec, design tone)
  </read_first>
  <files>
    C:/Users/Liamb/bolusbrain-landing/src/components/sections/Hero.tsx,
    C:/Users/Liamb/bolusbrain-landing/src/components/sections/DemoSection.tsx,
    C:/Users/Liamb/bolusbrain-landing/src/components/sections/Footer.tsx,
    C:/Users/Liamb/bolusbrain-landing/src/app/page.tsx
  </files>
  <action>
CREATE C:/Users/Liamb/bolusbrain-landing/src/components/sections/Hero.tsx:
```tsx
import { EmailForm } from '@/components/EmailForm'
import { IPhoneMockup } from '@/components/IPhoneMockup'

export function Hero() {
  return (
    <section className="min-h-screen flex flex-col justify-center bg-gray-950 px-6 lg:px-16 py-12">
      <div className="max-w-6xl mx-auto w-full grid lg:grid-cols-2 gap-12 items-center">

        {/* Left column: copy + email form */}
        <div className="flex flex-col gap-6">
          {/* Eyebrow */}
          <p className="text-red-500 text-sm font-semibold uppercase tracking-widest">
            For Type 1 Diabetics
          </p>

          {/* Headline */}
          <h1 className="text-4xl lg:text-5xl font-bold text-white leading-tight">
            See what your glucose
            <br />
            <span className="text-red-500">actually does</span> after meals
          </h1>

          {/* Subtext */}
          <p className="text-gray-400 text-lg leading-relaxed max-w-md">
            BolusBrain tracks your meals, insulin, and post-meal glucose curves.
            No advice. No recommendations. Just your data — so you can see the patterns.
          </p>

          {/* Legal note */}
          <p className="text-gray-600 text-xs">
            For personal pattern awareness only. Not a medical device. Always follow your diabetes team&apos;s guidance.
          </p>

          {/* Email capture */}
          <div className="flex flex-col gap-2">
            <p className="text-gray-300 text-sm font-medium">
              Get notified when it&apos;s ready for wider testing:
            </p>
            <EmailForm />
          </div>
        </div>

        {/* Right column: hero iPhone mockup */}
        <div className="hidden lg:flex justify-center items-center">
          <IPhoneMockup
            src="/screenshots/placeholder-1.png"
            alt="BolusBrain home screen showing live glucose reading and meal history"
          />
        </div>

      </div>
    </section>
  )
}
```

CREATE C:/Users/Liamb/bolusbrain-landing/src/components/sections/DemoSection.tsx:
```tsx
import { IPhoneMockup } from '@/components/IPhoneMockup'

export function DemoSection() {
  return (
    <section className="min-h-screen bg-gray-900 px-6 lg:px-16 py-16 flex flex-col justify-start">
      <div className="max-w-6xl mx-auto w-full">

        {/* Section heading */}
        <div className="mb-12 text-center">
          <h2 className="text-3xl lg:text-4xl font-bold text-white mb-3">
            Point. Estimate. Log.
          </h2>
          <p className="text-gray-400 text-lg max-w-xl mx-auto">
            Take a photo of your meal. BolusBrain estimates the carbs using UK food standards — then tracks how your glucose responds over 3 hours.
          </p>
        </div>

        {/* Three columns: demo mockup 1, demo mockup 2, Dexcom teaser */}
        <div className="grid lg:grid-cols-3 gap-8 items-start">

          {/* Demo step 1: photo capture */}
          <div className="flex flex-col items-center gap-4">
            <IPhoneMockup
              src="/screenshots/placeholder-2.png"
              alt="BolusBrain meal log screen — take a photo of your meal"
            />
            <div className="text-center">
              <p className="text-white font-semibold">Photograph your meal</p>
              <p className="text-gray-500 text-sm mt-1">
                Photo goes to AI. Carb estimate comes back in seconds, using UK CoFID food standards.
              </p>
            </div>
          </div>

          {/* Demo step 2: AI carb estimate result */}
          <div className="flex flex-col items-center gap-4">
            <IPhoneMockup
              src="/screenshots/placeholder-3.png"
              alt="BolusBrain AI carb estimation result screen"
            />
            <div className="text-center">
              <p className="text-white font-semibold">See the carb estimate</p>
              <p className="text-gray-500 text-sm mt-1">
                Log your insulin units alongside it. The app tracks what your glucose did next.
              </p>
            </div>
          </div>

          {/* Dexcom teaser card */}
          <div className="flex flex-col items-center gap-4">
            <div className="w-[240px] rounded-[2rem] bg-gray-800 border border-gray-700 p-6 flex flex-col gap-4 shadow-xl">
              {/* Coming soon badge */}
              <div className="flex items-center gap-2">
                <span className="bg-blue-900/60 text-blue-300 text-xs font-semibold px-3 py-1 rounded-full border border-blue-700/40">
                  Coming soon
                </span>
              </div>

              {/* Dexcom logo placeholder */}
              <div className="h-10 flex items-center">
                <span className="text-white font-bold text-xl tracking-tight">Dexcom G7</span>
              </div>

              <p className="text-gray-400 text-sm leading-relaxed">
                Direct Dexcom CGM integration — no Nightscout bridge required. Real-time glucose data straight into BolusBrain.
              </p>

              <p className="text-gray-600 text-xs">
                Next integration on the roadmap.
              </p>
            </div>

            <div className="text-center w-[240px]">
              <p className="text-white font-semibold">Dexcom integration</p>
              <p className="text-gray-500 text-sm mt-1">
                Native Dexcom G7 support is next. No bridge required.
              </p>
            </div>
          </div>

        </div>
      </div>
    </section>
  )
}
```

CREATE C:/Users/Liamb/bolusbrain-landing/src/components/sections/Footer.tsx:
```tsx
export function Footer() {
  return (
    <footer className="bg-gray-950 border-t border-gray-800 px-6 lg:px-16 py-8">
      <div className="max-w-6xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4">
        <div>
          <p className="text-gray-500 text-sm">
            &copy; {new Date().getFullYear()} BolusBrain. Personal use only.
          </p>
          <p className="text-gray-700 text-xs mt-1">
            Not a medical device. Not a substitute for your diabetes care team.
          </p>
        </div>
        <p className="text-gray-700 text-xs text-right">
          Built for T1D, by T1D.
        </p>
      </div>
    </footer>
  )
}
```

REPLACE C:/Users/Liamb/bolusbrain-landing/src/app/page.tsx (overwrite the placeholder from plan 01):
```tsx
import { Hero } from '@/components/sections/Hero'
import { DemoSection } from '@/components/sections/DemoSection'
import { Footer } from '@/components/sections/Footer'

export default function Home() {
  return (
    <main>
      <Hero />
      <DemoSection />
      <Footer />
    </main>
  )
}
```

CRITICAL fold layout check — before finishing this task, verify the layout manually:
- Open localhost:3000 in browser
- Set browser window to 1280x800 (or use Chrome DevTools device toolbar at this size)
- Without scrolling: Hero headline, tagline, email form, AND the top of DemoSection (showing both iPhone mockups and the Dexcom card) should all be visible
- If the DemoSection is NOT visible at 1280x800 without scrolling, reduce the Hero section's min-h-screen padding or reduce mockup size further
- The key constraint: Hero uses min-h-screen, DemoSection starts immediately after — so DemoSection's top content is visible when the user looks at the bottom of the viewport while on the Hero
- Actually: MKTG-01 says "both visible without scrolling past fold on desktop" — this means the Demo section and Dexcom teaser must be within the FIRST viewport. Re-read CONTEXT.md decision: "The demo section starts at or just below the fold — demo screenshots visible without full scroll." This means visible after ONE scroll or at the bottom of the hero. The strict reading is: both sections must be reachable without multiple scroll interactions — a single scroll brings demo into view.

If MKTG-01 requires both sections strictly above the fold (zero scroll), restructure the Hero section to use a more compact layout so the demo section begins within the first viewport height. In that case:
- Make Hero a two-column grid where the entire page fits in one screen
- Place demo mockups and Dexcom teaser as smaller elements alongside the hero copy
- Contact in the plan notes: the CONTEXT.md states "demo section starts at or just below the fold — demo screenshots visible without full scroll" — this is compatible with the demo being visible when scrolled one viewport. Use judgment.
  </action>
  <verify>
    <automated>cd C:/Users/Liamb/bolusbrain-landing && test -f src/components/sections/Hero.tsx && test -f src/components/sections/DemoSection.tsx && test -f src/components/sections/Footer.tsx && grep -q "EmailForm" src/components/sections/Hero.tsx && grep -q "IPhoneMockup" src/components/sections/DemoSection.tsx && grep -q "Coming soon" src/components/sections/DemoSection.tsx && grep -q "Dexcom" src/components/sections/DemoSection.tsx && grep -q "Hero" src/app/page.tsx && grep -q "DemoSection" src/app/page.tsx && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/Hero.tsx exports `Hero` and imports both `EmailForm` and `IPhoneMockup`
    - src/components/sections/DemoSection.tsx exports `DemoSection` and contains "Coming soon" text
    - DemoSection contains "Dexcom" — the teaser is present
    - DemoSection uses `lg:grid-cols-3` — three-column layout on desktop
    - Hero uses `lg:grid-cols-2` — two-column layout on desktop
    - Hero uses `min-h-screen` class
    - src/app/page.tsx imports Hero, DemoSection, and Footer from their section paths
    - `npx tsc --noEmit` passes — no TypeScript errors
    - `npm run build` succeeds — no Next.js build errors
    - DemoSection does NOT contain language like "you should take", "recommended dose", or any dosing advice
    - Footer contains legal disclaimer: "Not a substitute for your diabetes care team"
  </acceptance_criteria>
  <done>All page sections built and composed. TypeScript clean. Build passes. Dark-themed landing page renders at localhost:3000.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` in C:/Users/Liamb/bolusbrain-landing/ — exits 0, no errors
2. `npx tsc --noEmit` — no type errors
3. `grep -r "loops.so" src/components/` — returns empty (client code never calls Loops.so directly)
4. `grep -r "NEXT_PUBLIC_" src/` — returns empty (no client-leaked secrets)
5. `grep -c "Coming soon" src/components/sections/DemoSection.tsx` — returns 1 or more
6. `grep "Dexcom" src/components/sections/DemoSection.tsx` — returns match
7. Visual: `npm run dev` → localhost:3000 → dark page loads, email form visible in hero, demo section visible on scroll
</verification>

<success_criteria>
- Landing page renders at localhost:3000 with dark theme
- Hero section: BolusBrain headline, tagline, email form, and iPhone mockup all present
- Demo section: two iPhone mockups + Dexcom teaser card in lg:grid-cols-3 layout
- Dexcom teaser has "Coming soon" badge — no false promises about availability
- Email form shows "You're on the list!" inline on success (requires LOOPS_API_KEY populated)
- No dosing advice or recommendation language anywhere on the page
- Footer has legal disclaimer
- `npm run build` passes
</success_criteria>

<output>
After completion, create `C:/Users/Liamb/bolusbrain-app/.planning/phases/06-route-to-market/06-02-SUMMARY.md`

Include:
- All component file paths created
- Copy decisions made (headline, tagline, legal text)
- Fold layout approach used (min-h-screen hero + demo section immediately after)
- Screenshot placeholder paths and note that user must supply actual screenshots
- Any TypeScript issues encountered and resolved
- Confirmation that no Loops.so URL appears in any client component
</output>
