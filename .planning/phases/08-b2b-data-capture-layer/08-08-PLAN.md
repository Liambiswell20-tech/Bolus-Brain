---
phase: 08-b2b-data-capture-layer
plan: 08
type: execute
wave: 4
depends_on: [08-03, 08-04, 08-05, 08-06, 08-07]
files_modified: []
autonomous: false
requirements: [B2B-01, B2B-02, B2B-03, B2B-04, B2B-05, B2B-06, B2B-07, B2B-08]
must_haves:
  truths:
    - "Full automated test suite passes green"
    - "All 11 equipmentProfile tests pass"
    - "All 6 timeInRange tests pass"
    - "All B2B requirement acceptance criteria are manually verified"
  artifacts:
    - path: "src/__tests__/equipmentProfile.test.ts"
      provides: "11 passing test cases"
    - path: "src/__tests__/timeInRange.test.ts"
      provides: "6 passing test cases"
  key_links:
    - from: "src/screens/MealLogScreen.tsx"
      to: "src/utils/equipmentProfile.ts"
      via: "getCurrentEquipmentProfile at save time"
      pattern: "getCurrentEquipmentProfile"
    - from: "App.tsx"
      to: "src/utils/timeInRange.ts"
      via: "handleForeground → storeDailyTIR"
      pattern: "storeDailyTIR"
---

<objective>
Run the full automated test suite and pause for human verification of all B2B acceptance criteria across all 8 requirements.

Purpose: Phase 8 has significant manual verification requirements — the onboarding gate, equipment change flow, meal stamping, hypo treatment sheet, and consent toggle all require human confirmation that the app behaves correctly end-to-end. This plan provides a structured checklist and gating checkpoint before the phase can be marked complete.

Output: Phase sign-off, SUMMARY confirming all requirements verified.
</objective>

<execution_context>
@C:/Users/Liamb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Liamb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-b2b-data-capture-layer/08-CONTEXT.md
@.planning/phases/08-b2b-data-capture-layer/08-VALIDATION.md
@.planning/phases/08-b2b-data-capture-layer/08-03-SUMMARY.md
@.planning/phases/08-b2b-data-capture-layer/08-04-SUMMARY.md
@.planning/phases/08-b2b-data-capture-layer/08-05-SUMMARY.md
@.planning/phases/08-b2b-data-capture-layer/08-06-SUMMARY.md
@.planning/phases/08-b2b-data-capture-layer/08-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite and verify green</name>
  <files></files>
  <read_first>
    - .planning/phases/08-b2b-data-capture-layer/08-VALIDATION.md (per-task verification map — confirm all automated checks)
  </read_first>
  <action>
Run the full test suite:
```bash
npx jest --watchAll=false 2>&1
```

All tests must pass. If any test is failing:
1. Read the failure output carefully
2. Identify which plan introduced the regression
3. Fix the specific failing test or implementation
4. Re-run until all tests pass

Specifically confirm:
- `npx jest --testPathPattern="equipmentProfile" --watchAll=false` — 11 passed, 0 failed
- `npx jest --testPathPattern="timeInRange" --watchAll=false` — 6 passed, 0 failed
- All previously passing tests still pass (no regressions)

Also run TypeScript type-check:
```bash
npx tsc --noEmit
```
Must exit 0 with no errors.
  </action>
  <verify>
    <automated>npx jest --watchAll=false 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --watchAll=false` exits 0 (all tests pass)
    - `npx jest --watchAll=false 2>&1 | grep "failed"` returns no matches (zero failures)
    - `npx jest --testPathPattern="equipmentProfile" --watchAll=false 2>&1 | grep "11 passed"` returns a match
    - `npx jest --testPathPattern="timeInRange" --watchAll=false 2>&1 | grep "6 passed"` returns a match
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Full test suite green; 17 new B2B tests pass; TypeScript compiles without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete B2B data capture layer across all 8 requirements:
- B2B-01: Equipment onboarding gate (EquipmentOnboardingScreen + App.tsx gate)
- B2B-02: Equipment changelog data model + utilities (equipmentProfile.ts, timeInRange.ts, equipment.ts)
- B2B-03: Equipment change confirmation modal (EquipmentChangeConfirmation.tsx)
- B2B-04: Equipment settings section in SettingsScreen (My Equipment section)
- B2B-05: Meal stamping with insulin_brand + delivery_method at save time + read-only chip
- B2B-06: "Treating a low?" button + HypoTreatmentSheet + App.tsx foreground recovery fetch
- B2B-07: Silent TIR calculation on foreground + 90-day rolling store (no UI)
- B2B-08: Data & Research consent toggle in SettingsScreen (versioned, OFF by default)
  </what-built>
  <how-to-verify>
Work through each check in order. Mark each as pass or describe the failure.

**B2B-01: Onboarding gate**
1. Clear AsyncStorage: In a React Native development build, run `AsyncStorage.clear()` from the console, or use Expo Dev Tools to clear app data, then reload
2. Confirm EquipmentOnboardingScreen appears full-screen before HomeScreen
3. Try to navigate back — confirm no back gesture possible
4. Confirm the "Continue" button is disabled/dimmed until all 4 mandatory pickers are answered
5. Select 'Insulin pump' as delivery method — confirm pen needle picker does NOT appear
6. Select 'Disposable pen' — confirm pen needle picker appears
7. Complete all 4 mandatory pickers → tap Continue → confirm HomeScreen loads
8. Reload app without clearing storage → confirm HomeScreen loads directly (no gate)

**B2B-02: Equipment changelog**
9. In Settings → My Equipment, tap any field → picker appears → select new value → EquipmentChangeConfirmation modal appears
10. Confirm → read `equipment_changelog` in AsyncStorage → confirm previous entry has `ended_at` and new entry has `started_at` equal to the same value

**B2B-03: Confirmation modal**
11. Tap any equipment field in Settings → picker → select new value → confirm modal shows old AND new values before committing
12. Tap Cancel → confirm equipment_changelog is unchanged

**B2B-04: Equipment settings section**
13. Settings screen shows "My Equipment" section with all active equipment fields displayed
14. Pen needle row hidden when delivery method is 'Insulin pump' or 'Syringe & vial'

**B2B-05: Meal stamping**
15. Open MealLog screen → confirm insulin brand chip visible below insulin units input
16. Log a meal → read `glucolog_meals` AsyncStorage key → confirm the most recent meal has `insulin_brand` and `delivery_method` fields present

**B2B-06: Hypo treatment log**
17. HomeScreen: confirm "Treating a low?" button visible between stats row and Log meal button, with red border
18. Tap button → HypoTreatmentSheet opens
19. Confirm field order: (1) current glucose read-only, (2) treatment type picker, (3) amount + unit inline, (4) Save/Cancel
20. Fill in fields → tap Save → read `hypo_treatments` AsyncStorage key → confirm record present with all fields

**B2B-07: TIR store (no UI — AsyncStorage read)**
21. Background the app, then foreground it
22. Read `daily_tir` AsyncStorage key → confirm a DailyTIR record for yesterday appears

**B2B-08: Data consent**
23. Settings → Data & Research → confirm toggle is OFF by default
24. Toggle ON → read `data_consent` AsyncStorage key → confirm `{ consented: true, consented_at: ..., version: "1.0" }`
25. Toggle OFF → read key → confirm `{ consented: false, version: "1.0" }` (no consented_at)
26. Manually set `data_consent` to `{ "consented": true, "version": "0.9" }` in AsyncStorage → navigate to Settings → confirm re-consent modal appears
  </how-to-verify>
  <resume-signal>Type "approved" if all 26 checks pass. Otherwise describe which checks failed and what was observed — I will fix and you can re-verify those checks only.</resume-signal>
</task>

</tasks>

<verification>
All automated tests pass. All 26 manual verification checks confirmed by user.
</verification>

<success_criteria>
- `npx jest --watchAll=false` exits 0 with all 17 B2B tests passing (11 equipmentProfile + 6 timeInRange)
- `npx tsc --noEmit` exits 0
- User approves all 26 manual verification checks across B2B-01 through B2B-08
- Phase 8 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/08-b2b-data-capture-layer/08-08-SUMMARY.md`

Update STATE.md: phase 8 complete.
Update ROADMAP.md: Phase 8 row → Complete, date today.
Update REQUIREMENTS.md: B2B-01 through B2B-08 → Complete.
</output>
