---
phase: 09-pre-beta-polish
plan: 06
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/screens/HomeScreen.tsx
  - src/screens/MealLogScreen.tsx
  - src/screens/InsulinLogScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/components/HypoTreatmentSheet.tsx
autonomous: true
requirements: [BETA-07]

must_haves:
  truths:
    - "Save buttons are visible above keyboard on all screens"
    - "KeyboardAvoidingView uses consistent behavior prop per platform"
    - "ScrollViews use keyboardShouldPersistTaps='handled'"
    - "No screen uses hardcoded #000 for background"
  artifacts:
    - path: "src/screens/MealLogScreen.tsx"
      provides: "Standardised KeyboardAvoidingView with keyboardVerticalOffset"
      contains: "keyboardVerticalOffset"
    - path: "src/screens/InsulinLogScreen.tsx"
      provides: "Standardised KeyboardAvoidingView"
      contains: "keyboardVerticalOffset"
    - path: "src/screens/SettingsScreen.tsx"
      provides: "Standardised KeyboardAvoidingView"
      contains: "keyboardShouldPersistTaps"
  key_links:
    - from: "all screen files"
      to: "KeyboardAvoidingView"
      via: "consistent behavior and offset props"
      pattern: "keyboardVerticalOffset"
---

<objective>
Standardise KeyboardAvoidingView configuration across all screens with text inputs to ensure save buttons are never obscured by the keyboard. Fix any remaining hardcoded #000 backgrounds.

Purpose: Beta testers will use the app one-handed at mealtimes. If save buttons disappear behind the keyboard, they cannot complete the primary workflow.
Output: Consistent keyboard handling across MealLogScreen, InsulinLogScreen, SettingsScreen, and HypoTreatmentSheet.
</objective>

<execution_context>
@C:/Users/Liamb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Liamb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-pre-beta-polish/09-CONTEXT.md
@.planning/phases/09-pre-beta-polish/09-RESEARCH.md

<interfaces>
Current KeyboardAvoidingView patterns across screens:

HomeScreen.tsx: behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
MealLogScreen.tsx: behavior={Platform.OS === 'ios' ? 'padding' : undefined}
InsulinLogScreen.tsx: behavior={Platform.OS === 'ios' ? 'padding' : undefined}
HypoTreatmentSheet.tsx: behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
SettingsScreen.tsx: behavior={Platform.OS === 'ios' ? 'padding' : undefined}

Standard fix per RESEARCH.md:
- Screens with ScrollView children: behavior={Platform.OS === 'ios' ? 'padding' : undefined} with keyboardShouldPersistTaps="handled" on ScrollView + keyboardVerticalOffset on iOS
- Modal sheets: behavior={Platform.OS === 'ios' ? 'padding' : 'height'} (already correct)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardise KeyboardAvoidingView on MealLogScreen and InsulinLogScreen</name>
  <files>src/screens/MealLogScreen.tsx, src/screens/InsulinLogScreen.tsx</files>
  <read_first>
    - src/screens/MealLogScreen.tsx
    - src/screens/InsulinLogScreen.tsx
    - src/theme.ts
  </read_first>
  <action>
    **In src/screens/MealLogScreen.tsx:**

    1. Find the KeyboardAvoidingView component. Add `keyboardVerticalOffset` prop for iOS to account for the navigation header height:
       ```typescript
       <KeyboardAvoidingView
         style={{ flex: 1, backgroundColor: COLORS.background }}
         behavior={Platform.OS === 'ios' ? 'padding' : undefined}
         keyboardVerticalOffset={Platform.OS === 'ios' ? 100 : 0}
       >
       ```
       (100 accounts for the native stack navigator header bar ~ 44px + status bar ~ 50px + small buffer)

    2. Find the ScrollView inside and add `keyboardShouldPersistTaps="handled"` if not already present.

    3. If the root View or KeyboardAvoidingView style has `backgroundColor: '#000'`, change to `backgroundColor: COLORS.background`. Import COLORS from '../theme' if needed.

    **In src/screens/InsulinLogScreen.tsx:**

    1. Same changes as MealLogScreen: add `keyboardVerticalOffset={Platform.OS === 'ios' ? 100 : 0}` to KeyboardAvoidingView.

    2. Add `keyboardShouldPersistTaps="handled"` to any ScrollView.

    3. Change any `backgroundColor: '#000'` to `COLORS.background`. Import COLORS if needed.

    DO NOT change any other logic, state, or functionality in these files.
    DO NOT remove or modify any existing handler functions.
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/MealLogScreen.tsx contains `keyboardVerticalOffset`
    - src/screens/MealLogScreen.tsx contains `keyboardShouldPersistTaps`
    - src/screens/InsulinLogScreen.tsx contains `keyboardVerticalOffset`
    - src/screens/InsulinLogScreen.tsx contains `keyboardShouldPersistTaps`
    - Neither file contains `backgroundColor: '#000'`
    - npx jest --bail exits 0
  </acceptance_criteria>
  <done>MealLogScreen and InsulinLogScreen have standardised KeyboardAvoidingView with keyboardVerticalOffset=100 on iOS, keyboardShouldPersistTaps on ScrollViews, and COLORS.background for backgrounds.</done>
</task>

<task type="auto">
  <name>Task 2: Standardise KeyboardAvoidingView on SettingsScreen and fix remaining #000 backgrounds</name>
  <files>src/screens/SettingsScreen.tsx, src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx
    - src/screens/HomeScreen.tsx
    - src/theme.ts
  </read_first>
  <action>
    **In src/screens/SettingsScreen.tsx:**

    1. The KeyboardAvoidingView already has `behavior={Platform.OS === 'ios' ? 'padding' : undefined}`. Add `keyboardVerticalOffset`:
       ```typescript
       <KeyboardAvoidingView
         style={{ flex: 1, backgroundColor: COLORS.background }}
         behavior={Platform.OS === 'ios' ? 'padding' : undefined}
         keyboardVerticalOffset={Platform.OS === 'ios' ? 100 : 0}
       >
       ```

    2. Add `keyboardShouldPersistTaps="handled"` to the ScrollView.

    3. Verify no `backgroundColor: '#000'` remains (Plan 01 should have fixed these, but verify).

    **In src/screens/HomeScreen.tsx:**

    1. Scan for any `backgroundColor: '#000'` in the styles. Change to `COLORS.background` if found. Import COLORS if needed.
       Note: HomeScreen uses `behavior={Platform.OS === 'ios' ? 'padding' : 'height'}` — this is correct for HomeScreen as it has modal sheets, NOT ScrollView children. Do NOT change the behavior prop on HomeScreen.

    DO NOT change HomeScreen's KAV behavior prop.
    DO NOT change any handler functions or logic.
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/SettingsScreen.tsx contains `keyboardVerticalOffset`
    - src/screens/SettingsScreen.tsx contains `keyboardShouldPersistTaps`
    - src/screens/HomeScreen.tsx does NOT contain `backgroundColor: '#000'` (or already uses COLORS.background)
    - npx jest --bail exits 0
  </acceptance_criteria>
  <done>SettingsScreen has standardised keyboard handling. HomeScreen backgrounds use COLORS.background. All screens with text inputs now have consistent KeyboardAvoidingView configuration to keep save buttons visible.</done>
</task>

</tasks>

<verification>
- `npx jest --bail` passes
- `grep -rn "backgroundColor: '#000'" src/screens/` returns 0 matches (all screens use COLORS.background)
- `grep -c "keyboardVerticalOffset" src/screens/MealLogScreen.tsx src/screens/InsulinLogScreen.tsx src/screens/SettingsScreen.tsx` shows 1 match per file
</verification>

<success_criteria>
- MealLogScreen, InsulinLogScreen, and SettingsScreen all have keyboardVerticalOffset on iOS
- All ScrollViews in form screens have keyboardShouldPersistTaps="handled"
- No screen file uses hardcoded #000 for background
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-pre-beta-polish/09-06-SUMMARY.md`
</output>
